# TODO

- [ ] Fix React warning in `src/components/CreativeSelector.jsx`: duplicate keys in list rendering.
  - [ ] Replace `key={opt.template}` with a unique key (e.g., include index) to guarantee uniqueness.
  - [ ] Re-run the generate → select design → branding updates → preview updates → logo renders flow to confirm warnings are gone.

