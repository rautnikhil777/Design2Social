# TODO

- [ ] Create edit plan after inspecting relevant reel generation files.
- [ ] Inspect `frontend/src/pages/DashboardPage.jsx` reel generation + video field mapping.
- [ ] Inspect `backend/src/controllers/reelController.js` and reel service to confirm exact `/api/reel/generate` response shape.
- [ ] Add minimal temporary debug UI under reel preview: show `aiVideo` and `finalVideoSrc`/video src as text.
- [ ] Add temporary video event logs (`onLoadStart`, `onLoadedMetadata`, `onCanPlay`, `onError`, `onStalled`).
- [ ] Patch smallest possible backend/frontend mismatch if field names differ.
- [ ] Run frontend lint/build/test (if available) and validate reel preview renders.

